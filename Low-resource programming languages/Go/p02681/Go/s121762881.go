package main

import(
"fmt"
)

func main() {
  var s string
  var t string
  fmt.Scan(&s,&t)
  
  length := len(s)
 if ss[0:length-1] == tt[0:length-1] && length+1 == len(tt) {
    fmt.Println("Yes")
 }else{
   
    fmt.Println("No")
 }
}